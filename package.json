{
  "name": "ngrbet",
  "version": "0.3.0",
  "private": true,
  "description": "The casino where losing was never drawn. No money, no losing, still a draft.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview --open"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
