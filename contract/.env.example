# Deployer wallet (never commit the real .env)
PRIVATE_KEY=0x...
# Backend signer key: authorises claims. Can be a different wallet from the deployer.
SIGNER_PRIVATE_KEY=0x...
# Robinhood Chain RPCs
ROBINHOOD_RPC=https://rpc.mainnet.chain.robinhood.com
ROBINHOOD_TESTNET_RPC=https://rpc.testnet.chain.robinhood.com

# Deployment parameters
THRESHOLD_POINTS=10000        # min points to claim
POINTS_FOR_MAX=1000000        # points at which the payout reaches MAX_PAYOUT_ETH
MIN_PAYOUT_ETH=0.001
MAX_PAYOUT_ETH=0.1
COOLDOWN_SECONDS=86400        # one claim per wallet per 24h
FUND_ETH=0                    # optional: ETH to send to the contract right after deploy

# Backend (signer + points ledger + game host)
CONTRACT_ADDRESS=0x...
RPC_URL=https://rpc.testnet.chain.robinhood.com
CHAIN_ID=46630
API_KEY=change-me             # required on POST /points/add
PORT=8787
SESSION_SECRET=               # random string for login tokens (derived from signer key if empty)
POINTS_PER_ROUND=100          # points awarded per round played
EARN_COOLDOWN_SECONDS=8       # min seconds between two awarded rounds per wallet
DAILY_POINTS_CAP=20000        # max points a wallet can farm per day
VOUCHER_TTL_SECONDS=3600
SERVE_GAME=1                  # serve index.html + assets/ from this server
CORS_ORIGIN=*                 # or https://ton-site.com,https://autre.com
